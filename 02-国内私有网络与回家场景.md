---
title: 国内、私有网络与回家场景
area: project
purpose: design
lifecycle: draft
created: 2026-06-17
updated: 2026-06-17
tags:
  - openclash
  - clash
  - rules
  - 回家代理
---

# 国内、私有网络与回家场景

## 零、项目重写原则

1. **不依赖外部仓库**：所有规则在本项目内自行维护创建，不依赖第三方规则仓库。
2. **按组审查确认**：每一组规则先确认设计，再进入完整配置。
3. **最小可用原则**：只保留必要的规则，不盲目复制大量个人偏好列表。

## 一、核心背景

### 1.1 网络环境梳理

| 组件 | 说明 |
|------|------|
| **iStoreOS 软路由** | 10.10.10.20，运行 PassWall Server + Lucky 反代 |
| **PassWall Server** | 端口 12320，提供回家翻墙代理 |
| **Cloudflare DNS** | `*.l-home.top` 解析到 iStoreOS 的 IPv6 地址 |
| **公网情况** | 家里只有公网 IPv6，没有公网 IPv4 |
| **手机 Clash Mi** | Mihomo 内核，`mixed-port: 7890`，`allow-lan: false` |

### 1.2 回家场景核心问题

手机回家时如果忘记关 Clash Mi，会出现：
- 局域网访问异常（走了代理）
- 回家代理域名本身走代理，形成回环

**解决方案核心思路：私有网络 + 回家域名强制直连。**

---

## 二、最终需要产出的三个配置

| 配置 | 场景 | 核心需求 |
|------|------|----------|
| **1. 本地 OpenClash** | 家里 iStoreOS 上运行 | 家庭网关，正常分流翻墙 |
| **2. 手机回家配置** | 手机回家时用 | 通过家里 PassWall Server 翻墙，同时确保局域网访问正常 |
| **3. 手机机场配置** | 手机外出时用 | 直接连机场节点，不需要回家代理 |

---

## 三、三个配置的共性与差异

### 3.1 基础策略组（三个配置完全相同）

见 `01-基础策略组与通用选择.md`，共用：
- 业务组默认第一项为「通用选择」
- 「通用选择」默认第一项为「所有-自动」
- 「直连」和「拒绝」放在靠后位置
- 所有 `include-all` 普通节点池提前排除 `链式代理\|住宅`

### 3.2 本分组差异对比

| 规则/模块 | 本地 OpenClash | 手机回家 | 手机机场 |
|-----------|---------------|---------|---------|
| **IPv4 私有网络直连** | ✅ | ✅ | ✅ |
| **IPv6 私有网络直连** | ✅ | ✅ | ✅ |
| **GEOIP,CN 国内直连** | ✅ | ✅ | ✅ |
| **`*.l-home.top` 强制直连** | ❌ | ✅ | ❌ |
| **回家代理 `Home` 定义** | ❌ | ✅ | ❌ |
| **`proxy-server-nameserver` 国内 DNS** | ✅ | ✅ | ✅ |
| **IPv6 DNS 解析** | ✅ | ✅ | ✅ |

---

## 四、具体规则设计

### 4.1 私有网络直连规则（三个配置都有）

```yaml
# IPv4 私有网络
- IP-CIDR,10.0.0.0/8,DIRECT,no-resolve
- IP-CIDR,172.16.0.0/12,DIRECT,no-resolve
- IP-CIDR,192.168.0.0/16,DIRECT,no-resolve

# IPv6 私有网络
- IP-CIDR,fc00::/7,DIRECT,no-resolve
- IP-CIDR,fe80::/10,DIRECT,no-resolve

# 回环地址
- IP-CIDR,127.0.0.0/8,DIRECT,no-resolve
- IP-CIDR,::1/128,DIRECT,no-resolve
```

**关键：** `no-resolve` 参数，避免解析 IP 时走代理 DNS。

### 4.2 国内域名直连规则（三个配置都有）

```yaml
- GEOIP,CN,DIRECT
```

### 4.3 回家域名直连规则（仅手机回家配置）

```yaml
# 家里反代域名强制直连，避免回环
- DOMAIN-SUFFIX,l-home.top,DIRECT
```

### 4.4 回家代理定义（仅手机回家配置）

```yaml
proxies:
  - name: Home
    type: ss
    server: socks.l-home.top
    port: 12320
    cipher: aes-256-gcm
    password: Yx880721
    # udp: true  # 可选，根据需要开启
```

### 4.5 DNS 配置（三个配置都有，手机回家需要特别注意）

```yaml
dns:
  enable: true
  ipv6: true  # 关键！启用 AAAA 解析，否则回家 IPv6 域名解析失败

  # 用国内 DNS 解析代理节点域名，避免鸡蛋问题
  proxy-server-nameserver:
    - https://doh.pub/dns-query

  # 其他 DNS 配置根据实际情况补充
```

**手机回家配置特别注意：**
- `ipv6: true` 必须开启，否则 `socks.l-home.top` 解析不到 IPv6 地址
- `proxy-server-nameserver` 用国内 DoH，确保能正确解析国内 IPv6 地址

---

## 五、规则顺序与优先级

三个配置的规则顺序建议：

```yaml
rules:
  # 1. 最高优先级：私有网络（必须第一组，避免被其他规则覆盖）
  - IP-CIDR,10.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,172.16.0.0/12,DIRECT,no-resolve
  - IP-CIDR,192.168.0.0/16,DIRECT,no-resolve
  - IP-CIDR,fc00::/7,DIRECT,no-resolve
  - IP-CIDR,fe80::/10,DIRECT,no-resolve
  - IP-CIDR,127.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,::1/128,DIRECT,no-resolve

  # 2. 回家域名（仅手机回家配置）
  - DOMAIN-SUFFIX,l-home.top,DIRECT

  # 3. 广告/拦截规则（第 3 组补充）

  # 4. 业务分流规则（后续分组补充）

  # 5. 国内直连
  - GEOIP,CN,DIRECT

  # 6. 兜底
  - MATCH,通用选择
```

---

## 六、关键注意事项

### 6.1 回家场景的常见坑

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 回家时局域网访问异常 | 私有网络 IP 走了代理 | 私有网络规则放在最前面，加 `no-resolve` |
| `socks.l-home.top` 连接失败 | DNS 没有启用 IPv6 | `ipv6: true` |
| 回家代理本身走了代理 | 回家域名被匹配到其他规则 | `*.l-home.top` 直连规则放在国内规则之前 |
| 手机切换 Wi-Fi/蜂窝后连接异常 | 网络切换导致连接状态异常 | Clash Mi 一般会自动重连，手动开关一次即可 |

### 6.2 规则优先级原则

1. **私有网络 > 特定域名 > 广告拦截 > 业务分流 > 国内 > 兜底**
2. **`no-resolve` 对 IP 规则非常重要**，避免解析 IP 时走代理 DNS
3. 回家域名规则必须放在国内规则之前，因为 `l-home.top` 是国内域名，但必须确保它直连

### 6.3 配置切换建议

手机端建议准备两个配置文件：
- **home.yaml** —— 回家时用，包含 `Home` 代理和 `*.l-home.top` 直连
- **mobile.yaml** —— 外出时用，直接连机场节点

回家时切换到 `home.yaml`，外出时切换到 `mobile.yaml`。

---

## 七、待确认

| # | 设计点 | 确认 |
|---|---------|------|
| 1 | 私有网络规则顺序放在最前面，加 `no-resolve` | ⬜ |
| 2 | 手机回家配置单独加 `*.l-home.top` 直连规则 | ⬜ |
| 3 | 手机回家配置加 `Home` SS 代理定义 | ⬜ |
| 4 | 三个配置都开启 `ipv6: true` | ⬜ |
| 5 | 三个配置都用 `proxy-server-nameserver` 国内 DoH | ⬜ |
| 6 | 最终产出三个独立配置文件：本地、手机回家、手机机场 | ⬜ |

---

## 八、后续计划

确认本分组后，继续第 3 组：广告/拦截规则。

---

## 来源

- 用户提供的 Clash Mi 配置片段
- Clash Mi 官方文档：https://clashmi.app/guide/
- Mihomo 配置参考：https://clashmi.app/wiki/configuration-reference
- 项目 README：[[README|OpenClash 规则重写]]
- 项目状态：[[STATUS|OpenClash 规则重写状态]]
